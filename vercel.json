{
    "version": 2,

    "builds": [
      {
        "src": "app/main.py",
        "use": "@vercel/python",
        "config": {
          "runtime": "python3.11",
          "memory": 1024,
          "maxDuration": 30
        }
      }
    ],
  
    "installCommand": "pip install --no-cache-dir -r requirements-prod.txt --target .",

    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "app/main.py" }
    ]
  }